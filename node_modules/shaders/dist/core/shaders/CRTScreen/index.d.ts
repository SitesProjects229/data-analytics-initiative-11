import { ComponentDefinition } from '../../types';

export interface ComponentProps {
    /**
     * Size of individual TV pixels (lower = more pixels)
     *
     * Accepts a number between 8 and 128.
     * @default 128
     */
    pixelSize: number;
    /**
     * Chromatic aberration amount
     *
     * Accepts a number between 0 and 10.
     * @default 1
     */
    colorShift: number;
    /**
     * Strength of horizontal scanlines
     *
     * Accepts a number between 0 and 1.
     * @default 0.3
     */
    scanlineIntensity: number;
    /**
     * Number of scanlines across screen
     *
     * Accepts a number between 100 and 800.
     * @default 200
     */
    scanlineFrequency: number;
    /**
     * Screen brightness boost
     *
     * Accepts a number between 0.5 and 2.
     * @default 1.1
     */
    brightness: number;
    /**
     * Screen contrast enhancement
     *
     * Accepts a number between 0.5 and 2.
     * @default 1.2
     */
    contrast: number;
}
export declare const componentDefinition: ComponentDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map