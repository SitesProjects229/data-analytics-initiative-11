import { ComponentDefinition } from '../../types';
import { transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Frequency of the halftone dots
     *
     * Accepts a number between 10 and 300.
     * @default 100
     */
    frequency: number;
    /**
     * Rotation angle of the pattern (in degrees)
     *
     * Accepts a number between 0 and 360.
     * @default 45
     */
    angle: Parameters<typeof transformAngle>[0];
    /**
     * Edge smoothness of the dots
     *
     * Accepts a number between 0 and 0.3.
     * @default 0.1
     */
    smoothness: number;
}
export declare const componentDefinition: ComponentDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map