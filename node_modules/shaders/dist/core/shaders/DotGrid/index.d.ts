import { ComponentDefinition } from '../../types';
import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * The color of the dot
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#ffffff"
     */
    color: Parameters<typeof transformColor>[0];
    /**
     * The number of dots on the longest canvas edge
     *
     * Accepts a number between 10 and 500.
     * @default 100
     */
    density: number;
    /**
     * The size of each dot, zero (0) being invisible, one (1) filled the grid with no gaps
     *
     * Accepts a number between 0 and 1.
     * @default 0.3
     */
    dotSize: number;
    /**
     * Intensity of the twinkle effect (0 = off, 1 = full twinkle)
     *
     * Accepts a number between 0 and 1.
     * @default 0
     */
    twinkle: number;
}
export declare const componentDefinition: ComponentDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map