import { ComponentDefinition } from '../../types';

export interface ComponentProps {
    /**
     * Characters to use for ASCII effect (light to dark)
     *
     * Accepts a string value.
     * @default "@%#*+=-:."
     */
    characters: string;
    /**
     * Size of each ASCII character cell in pixels
     *
     * Accepts a number between 8 and 100.
     * @default 30
     */
    cellSize: number;
    /**
     * Font family for characters
     *
     * Accepts one of: `"JetBrains"`, `"Fira"`, `"Source"`, `"IBM"`, `"Space"`, `"Roboto"`, `"Courier"`.
     * @default "JetBrains Mono"
     */
    fontFamily: string;
    /**
     * Character size within each cell (1.0 = optimal size, 0.0 = smallest)
     *
     * Accepts a number between 0 and 1.
     * @default 1
     */
    spacing: number;
}
export declare const componentDefinition: ComponentDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map